jQuery ->
    # $(".stream li").each ->
    #     $(this).hover ->
    #         $(this).find(".desc").toggle()
    #         return
    #     return

    # if !$(document.body).hasClass("math")
    #     Hyphenator.run()
    # else
    #     MathJax.Hub.Register.StartupHook "End", ->
    #         Hyphenator.run()
    #         return

    return
